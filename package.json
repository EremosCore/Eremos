{
  "name": "eremos-core",
  "version": "0.1.0",
  "description": "Modular agent framework for on-chain activity monitoring.",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon --exec npx ts-node scripts/dev-agent.ts",
    "test": "echo 'No test runner configured. Add one (e.g. jest, mocha) to run tests.'",
    "coverage": "nyc npm test"
  },
  "keywords": [
    "agent",
    "onchain",
    "modular",
    "blockchain",
    "framework"
  ],
  "license": "MIT",
  "author": "EremosCore",
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
